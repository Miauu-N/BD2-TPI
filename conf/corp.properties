# Corp Node Configuration (Hub)
engine.name=corp-000
group.id=corp
external.id=000

# Database Connection (Firebird)
# Replace with actual path
db.driver=org.firebirdsql.jdbc.FBDriver
db.url=jdbc:firebirdsql://localhost:3050/c:/data/corp.fdb?encoding=UTF8
db.user=sysdba
db.password=masterkey

# Registration Server URL
# The hub listens for registrations
registration.url=http://localhost:31415/sync/corp-000
sync.url=http://localhost:31415/sync/corp-000

# Server Port
http.port=31415
jmx.port=31416

# Job Timings
job.purge.period.time.ms=7200000
job.routing.period.time.ms=5000
job.push.period.time.ms=10000
job.pull.period.time.ms=10000

firebird.extract.varchar.row.old.pk.data=8000,8000,1000
