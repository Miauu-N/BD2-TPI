# Hotel Node Configuration (Spoke Template)
# Replace 'hotel-001' and '001' with specific hotel ID
engine.name=hotel-001
group.id=hotel
external.id=1

# Database Connection (Firebird)
# Replace with actual path
db.driver=org.firebirdsql.jdbc.FBDriver
db.url=jdbc:firebirdsql://localhost:3050/D:/escu/Bd/bd2/TPI/H1.fdb?encoding=UTF8
db.user=sysdba
db.password=masterkey

# Registration URL
# Points to the Corp node
registration.url=http://localhost:31415/sync/corp-000

# Sync URL (Local listener)
sync.url=http://localhost:31415/sync/hotel-001

# Server Port (Must be unique if running on same machine as Corp for testing)
http.port=31415
jmx.port=8081

# Job Timings
job.routing.period.time.ms=5000
job.push.period.time.ms=10000
job.pull.period.time.ms=10000
firebird.extract.varchar.row.old.pk.data=8000,8000,1000
